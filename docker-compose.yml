services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: nexgear_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: nexgear123
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
      - media_data:/app/media
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgres://postgres:nexgear123@db:5432/nexgear_db
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
  media_data:
